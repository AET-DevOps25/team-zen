spring.application.name=user-microservice-test
# Test configuration
spring.data.mongodb.auto-index-creation=true
server.port=0
logging.level.org.springframework.web=DEBUG
logging.level.com.example.user_microservice=DEBUG

# MongoDB configuration will be provided by Testcontainers in integration tests
# For unit tests that don't need MongoDB, use @WebMvcTest or exclude MongoDB components
